# TODO

## 1. 标注功能（录制 + 回放）

### 需求描述
在讲解时，支持右击呼出标注工具栏，提供以下功能：
- **高亮**：黄色背景高亮选中文字
- **下划线**：给选中文字加下划线
- **删除线**：给选中文字加删除线
- **批注**：添加文本批注（气泡形式显示）
- **问号**：标记疑问点（图标标记）
- **贴纸**：插入表情/贴纸图标
这些功能将形成接口，未来给 AI 讲解调用。

### 技术要求
- 右击呼出浮动工具栏（类似 Notion / Figma 的 contextual toolbar）
- 所有标注操作需记录到时间轴 `states` 或新增 `annotations` 字段
- 录制时保存：类型、字符偏移量（md）或坐标位置（pdf）、内容（批注文本）、时间戳
- 播放时按时间还原标注（高亮/下划线/删除线用 CSS class，批注用绝对定位气泡）
- 兼容多文件切换场景（标注绑定到具体文件的特定位置）

### 数据结构设计
```json
{
  "annotations": [
    {
      "t": 12500,
      "type": "highlight|underline|strike|comment|question|sticker",
      "fi": 0,
      "offsets": [a, b],
      "text": "批注内容（仅 comment 类型）",
      "icon": "❓|⭐|..."（仅 sticker 类型）
    }
  ]
}
```

---

## 2. UI 美化与折叠

### 目录折叠
- 左侧目录支持点击标题前的 `▶/▼` 图标折叠/展开子级
- h1 可折叠其下的 h2/h3；h2 可折叠其下的 h3
- 默认全部展开
- 折叠状态不持久化（刷新重置）

### 顶部工具栏折叠
- 录制栏和播放栏占用垂直空间过多，影响文档阅读区域
- 增加"收起工具栏"按钮（小箭头图标），点击后工具栏高度收缩至最小（只保留核心按钮）
- 再次点击恢复完整高度
- 鼠标悬停到收起的工具栏上时自动临时展开（可选）

<!-- ### Markdown 正文排版优化
- 增大正文字号（当前可能偏小）
- 调整行高至 1.8~2.0
- 标题间距优化（h1/h2/h3 上下 margin）
- 代码块背景色与边框优化
- 引用块（blockquote）样式美化
- 表格斑马纹或边框优化
- 图片最大宽度限制为容器宽度，居中显示 -->

---

## 3. AI 语音讲解 Demo

### 目标
写一个最小的 demo，验证用 AI 自动生成语音讲解的可行性。

### 技术方案
- **TTS 引擎**：浏览器原生 `SpeechSynthesis` API（无需后端，零依赖）
- **输入**：选中的 md 段落或全文
- **流程**：
  1. 用户选中一段文字
  2. 点击"AI 讲解"按钮（或快捷键）
  3. 调用 `speechSynthesis.speak()` 朗读选中内容
  4. 可选：用简单的规则提取关键句（如以句号分隔的第一句）作为"讲解摘要"

### 扩展方向（后续）
- 接入云端 TTS（Azure Speech / 阿里云智能语音）获得更自然的音色
- 结合 LLM 生成讲解脚本（把技术文档转成口语化讲解）
- 与录制流程集成：录制时可选择"AI 辅助讲解"模式，自动生成旁白音频轨道

### 交付物
- 独立的 `ai-demo.html` 文件（单页，无依赖）
- 或直接在 `lecture-lite.html` 中加一个实验性按钮（隐藏式，供内部测试）
